const initialState = {
    texts: [],
 }

describe('CopyWrite App', function() {
    it('should have an initialState that contains an empty array called texts', ()=>{
        expect(initialState.texts).toEqual([]);
    } );
});    

